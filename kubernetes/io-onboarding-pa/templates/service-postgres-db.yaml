---

apiVersion: v1

kind: Service
metadata:
  name: {{ template "io-onboarding-pa.fullname" . }}-postgres-db
  labels:
    app: {{ template "io-onboarding-pa.name" . }}
    chart: {{ template "io-onboarding-pa.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.ioOnboardingPa.services.postgresDb.type }}
  ports:
    - name: postgres-db
{{ toYaml .Values.ioOnboardingPa.services.postgresDb | indent 6 }}
  selector:
    app: {{ template "io-onboarding-pa.name" . }}
    release: {{ .Release.Name }}
